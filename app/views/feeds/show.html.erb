<section class="feed">
  <div class="feedName pageHeader">
    <h1><%= @feed.name %></h1>
    <%= link_to 'Edit', edit_feed_path(@feed) %>
  </div>
  <div class="feedItemContainer">
    <%= render 'feeds' %>
  </div>
</section>